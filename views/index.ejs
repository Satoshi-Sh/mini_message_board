<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1 id="header"> Message Board </h1>
    <div class="cards">
    <% for (let i=0; i< messages.length;i++){ %>
    <div class="card">
      <h2 class="user"><%= messages[i].user %></h2>
      <p class="message"><%= messages[i].text %></p>
      <p class="date"><%= messages[i].added %></p>
    </div>
    <%} %>
    <a href="/new">To Form</a>
  </body>
</html>
